<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    <link rel="stylesheet" href="/styles/styles.css">
</head>
<body>
    <h1>Welcome <%= locals.currentUser.first_name %></h1>
    <div class="messages-container">
        <% messages.forEach(message => { %>
            <%- include("partials/message", {message: message, isMember: isMember}) %>
        <% }) %>
    </div>
    <a href="/logout">Log Out</a>
</body>
</html>