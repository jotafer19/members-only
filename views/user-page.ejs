<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/styles/styles.css">
</head>
<body>
    <%- include("partials/navbar") %>
    <div class="home-container">
        <div class="home-content">
            <h1 class="index-title"><%= currentUser.id === messages[0].user_id ? "Your messages" : messages[0].first_name + " " + messages[0].last_name +"'s " + "messages" %></h1>
            <div class="messages-container">
                <% messages.forEach(message => { %>
                    <%- include("partials/message", {message: message, isMember: currentUser.membership_status}) %>
                <% }) %>
            </div>
        </div>
    </div>
</body>
</html>